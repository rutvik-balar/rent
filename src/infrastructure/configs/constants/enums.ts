export enum Validation_MSG {
  REQUIRED = 'is required',
  TOKEN_EXP = 'Token used too late',
  TOKEN_INVALID = 'Invalid token signature',
}

export enum USER_ROLES {
  USER = 'user',
  ADMIN = 'admin',
  SUPER_ADMIN = 'super_admin',
}

export enum TIME_UNITS {
  YEARS = 'y',
  QUARTERS = 'Q',
  MONTHS = 'M',
  WEEKS = 'w',
  DAYS = 'd',
  HOURS = 'h',
  MINUTES = 'm',
  SECONDS = 's',
  MILLISECONDS = 'ms',
}
